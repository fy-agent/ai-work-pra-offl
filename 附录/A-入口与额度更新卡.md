# 附录 A · 入口与额度更新卡

主文不写死按钮位置、套餐名、价格和额度。那些东西会变。开用前自己填一张当天的卡。

不要把填好的真实账号、账单、邀请码提交回本仓库。

```text
日期：

我用的工作台：

打开方式（应用 / 网页 / 编辑器插件，选实际有的）：

登录用的是个人账号还是机构席位：

今天能做：读文件 / 改文件 / 联网  （以实际权限为准）

今天不能做：

额度或用量在哪看：

额度不够时会发生什么（停 / 排队 / 另计费，选你看到的）：

我准备用来练习的文件夹：

练习材料是否有权使用：是 / 否
```

没有的项就写「当天界面没找到」，不要抄别人的旧教程凑。

主文里说的「三个区」「发送给工作台不是发给同事」，要靠你在这张卡对应的软件上自己指一遍。指不上，先不要开始改正式文件。

## 一次实机核对（会过期）

核对日：2026-08-20。软件：本机 WorkBuddy 桌面版，窗口标题 WorkBuddy，界面写 v5.3.14。这不是官方承诺，明天就可能改。

当时看得见的入口（只记种类，不记价格）：

- 左栏：新建任务、助理、项目、专家·技能·连接器、自动化、资料库、任务列表、空间
- 首页输入：今天帮你做些什么；@ 引用对话文件；/ 调用技能与指令
- 输入区下方：选择工作空间（可搜索已有空间、新建、打开本地文件夹）、默认权限
- 模式：日常办公 / 代码开发 / 设计创意
- 发送后：左栏出现该任务；中间出现准备中、深度思考、读取中；发送键变成停止
- 页脚有「内容由 AI 生成，请核实重要信息」

同日用虚构月报表和样例跑了一次。工作台去读文件，工作副本里找不到，就停下来问路径，没有编一份月报。结论写进第 13–16 章：没选进空间的材料，工具会失败；失败时不要改用编造交差。

模型名单、倍率、积分活动当天就在变，不要抄进主文，也不要提交进本仓库。
